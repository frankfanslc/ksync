---
# Source: crds/samplecrd.arhat.dev_foo.yaml

---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  creationTimestamp: null
  name: foo.samplecrd.arhat.dev
spec:
  additionalPrinterColumns:
  - JSONPath: .spec.bar
    name: Bar
    type: string
  - JSONPath: .status.bar
    name: Status-Bar
    type: string
  group: samplecrd.arhat.dev
  names:
    kind: Foo
    listKind: FooList
    plural: foo
    shortNames:
    - foo
    - foos
    singular: foo
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: Foo is the Schema for the foo API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: FooSpec defines the desired state of Foo
          properties:
            bar:
              type: string
          type: object
        status:
          description: FooStatus defines the observed state of Foo
          properties:
            bar:
              type: string
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []

---
# Source: template-kubernetes-controller/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: template-kubernetes-controller
  namespace: --debug
  labels:
    helm.sh/chart: template-kubernetes-controller-0.1.0
    app.kubernetes.io/name: template-kubernetes-controller
    app.kubernetes.io/instance: template-kubernetes-controller
    app.kubernetes.io/version: "latest"
    app.kubernetes.io/managed-by: Helm
---
# Source: template-kubernetes-controller/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: template-kubernetes-controller
  namespace: --debug
  labels:
    helm.sh/chart: template-kubernetes-controller-0.1.0
    app.kubernetes.io/name: template-kubernetes-controller
    app.kubernetes.io/instance: template-kubernetes-controller
    app.kubernetes.io/version: "latest"
    app.kubernetes.io/managed-by: Helm
data:
  config.yaml: |
    templateKubernetesController:
      foo: bar
      kubeClient:
        rateLimit:
          burst: 10
          enabled: true
          qps: 5
      leaderElection:
        lease:
          expiration: 30s
          expiryToleration: 10s
          renewDeadline: 29s
          renewInterval: 5s
        lock:
          type: configmaps
      log:
      - file: stderr
        format: console
        level: verbose
      metrics:
        enabled: true
        httpPath: /metrics
        listen: :0
---
# Source: template-kubernetes-controller/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: template-kubernetes-controller:--debug:discovery
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:discovery
subjects:
- kind: ServiceAccount
  name: template-kubernetes-controller
  namespace: --debug
---
# Source: template-kubernetes-controller/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: template-kubernetes-controller:controller
  namespace: --debug
rules:
- apiGroups: [""]
  resources:
  - configmaps
  verbs:
  - create
  - get
  - update
  - patch
  resourceNames:
  - template-kubernetes-controller-leader-election
---
# Source: template-kubernetes-controller/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: template-kubernetes-controller
  namespace: --debug
  labels:
    helm.sh/chart: template-kubernetes-controller-0.1.0
    app.kubernetes.io/name: template-kubernetes-controller
    app.kubernetes.io/instance: template-kubernetes-controller
    app.kubernetes.io/version: "latest"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: template-kubernetes-controller
      app.kubernetes.io/instance: template-kubernetes-controller
  template:
    metadata:
      labels:
        app.kubernetes.io/name: template-kubernetes-controller
        app.kubernetes.io/instance: template-kubernetes-controller
    spec:
      serviceAccountName: template-kubernetes-controller
      securityContext:
        {}
      volumes:
        - name: config
          configMap:
            name: template-kubernetes-controller
      containers:
        - name: controller
          securityContext:
            {}
          image: "docker.io/arhatdev/template-kubernetes-controller:latest"
          imagePullPolicy: Always
          command:
            - /template-kubernetes-controller
            - -c
            - /etc/template-kubernetes-controller/config.yaml
          env:
            - name: GODEBUG
              value: madvdontneed=1
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            {}
